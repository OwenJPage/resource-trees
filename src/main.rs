mod dose;
mod new_tree;
mod pill_count;
mod search_tree;

type DayType = u16;
type AmountType = u16;
type CountType = u8;

type SizeType = u32;
type HeightType = u16;

fn main() {
    todo!("Main function")
}
